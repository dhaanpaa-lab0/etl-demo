# Example environment variables for EtlDbConfig
#
# EtlDbConfig reads variables with a prefix:
# - Default (no env name):        PG_*
# - With env name (e.g., DEV):    PG_DEV_*
#
# Required keys per prefix:
#   - DB      : Database name (required)
#   - USER    : Username (defaults to OS $USER if not set)
#   - PASS    : Password (required for auth)
#   - HOST    : Hostname (default: localhost)
#   - PORT    : Port (default: 5532)
#   - TYPE    : SQLAlchemy DB type/driver (default: postgresql+psycopg)
#
# The resulting SQLAlchemy URI looks like:
#   TYPE://USER:PASS@HOST:PORT/DB

# --- Default connection (used when EtlDbConfig() is constructed without env name) ---
PG_DB=your_database
PG_USER=your_username
PG_PASS=your_password
# PG_HOST=localhost
# PG_PORT=5532
# PG_TYPE=postgresql+psycopg

# --- Dev environment example (used when EtlDbConfig("dev") is provided) ---
PG_DEV_DB=your_dev_database
PG_DEV_USER=your_dev_user
PG_DEV_PASS=your_dev_password
# PG_DEV_HOST=localhost
# PG_DEV_PORT=5532
# PG_DEV_TYPE=postgresql+psycopg

# --- Prod environment example (used when EtlDbConfig("prod") is provided) ---
PG_PROD_DB=your_prod_database
PG_PROD_USER=your_prod_user
PG_PROD_PASS=your_prod_password
# PG_PROD_HOST=db.example.com
# PG_PROD_PORT=5532
# PG_PROD_TYPE=postgresql+psycopg

# --- MySQL example ---
# PG_DB=your_database
# PG_USER=your_username
# PG_PASS=your_password
# PG_HOST=localhost
# PG_PORT=3306
# PG_TYPE=mysql+pymysql

# --- DuckDB example (file-based) ---
# PG_TYPE=duckdb
# PG_DB=/path/to/database.duckdb

# --- DuckDB example (in-memory) ---
# PG_TYPE=duckdb
# PG_DB=:memory:
